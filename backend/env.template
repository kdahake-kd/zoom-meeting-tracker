# Zoom API Credentials
# Get these from https://marketplace.zoom.us/develop/create
# After creating your OAuth app, copy the values here
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
ZOOM_ACCOUNT_ID=

# OAuth Redirect URI
# Must match exactly what you set in your Zoom App settings
# For local development, use: http://localhost:8000/auth/zoom/callback
ZOOM_REDIRECT_URI=http://localhost:8000/auth/zoom/callback

# Server Configuration
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=development

# Database Configuration
# SQLite database path (default: ./data/meetings.db)
DATABASE_URL=sqlite+aiosqlite:///./data/meetings.db

# Webhook Secret Token
# Set this in your Zoom App webhook settings (Feature > Webhook)
# Use a strong random string for security (optional for local development)
WEBHOOK_SECRET_TOKEN=

# Frontend URL (for CORS)
# The URL where your React frontend is running
FRONTEND_URL=http://localhost:3000

